<%= form_for(@common_space) do |f| %>
  <% if @common_space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@common_space.errors.count, "error") %> prohibited this common_space from being saved:</h2>

      <ul>
      <% @common_space.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :start %><br>
    <%= f.date_select :start %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
